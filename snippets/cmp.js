
module.exports =

`import React, {Component} from 'react';

class ` + "${1}" + ` extends Component {

  render(){
    return (
      <div>
        ` + "${2}" + `
      </div>
    );
  }
}

export default` + "${1}" + `;`
